PATH=/home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/index.ts
test -f RESULT=true
wc -l:
0 /home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/index.ts
sha256sum:
fa1eb3e0d355d3f125e6abfbc40e8c8dfdf0f98f7e5200599640e93b64d9bd43  /home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/index.ts
HEAD20:
import type { NextApiRequest, NextApiResponse } from \"next\";\nimport { prisma } from \"../../../../lib/prisma\";\nimport { ok, err } from \"@shared/types/api\";\n\nconst PACKAGE_MAP: Record<string, { price: number; templateCode: string; currency: string }> = {\n  bazi_basic: { price: 99, templateCode: \"bazi_basic\", currency: \"USD\" },\n  bazi_theme: { price: 199, templateCode: \"bazi_theme\", currency: \"USD\" },\n  bazi_consult: { price: 599, templateCode: \"bazi_consult\", currency: \"USD\" },\n};\n\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  if (req.method !== \"POST\") return res.status(405).end();\n\n  const userId = (req.headers[\"x-user-id\"] as string | undefined) ?? null;\n  if (!userId) return res.status(401).json(err(\"UNAUTHORIZED\", \"Unauthorized\"));\n\n  const body = req.body as any;\n  const { package_code, intake, appointment, consent } = body ?? {};\n  if (!package_code || !intake || !consent) {\n    return res.status(422).json(err(\"VALIDATION_ERROR\", \"Missing payload\"));\n  }\n\n  const requiredKeys = [\"disclaimer\",\"privacy\",\"refund\",\"cookie\"] as const;\n  const ids = consent?.consent_version_ids;\n  if (!ids || requiredKeys.some((k) => !ids[k])) {\n    return res.status(400).json(err(\"CONSENT_VERSION_MISSING\", \"Missing required consent_version_ids\", { requiredKeys }));\n  }\n\n  const { form_type, consent_disclaimer_version, consent_privacy_version, consent_refund_version, consent_cookie_version, consent_accepted_at, consent_version_ids } = consent;\n  if (!consent_accepted_at) return res.status(422).json(err(\"VALIDATION_ERROR\", \"Missing consent_accepted_at\"));\n\n  const requiredIdValues = [consent_version_ids.disclaimer, consent_version_ids.privacy, consent_version_ids.refund, consent_version_ids.cookie];\n  const rows = await prisma.consentVersion.findMany({\n    where: { id: { in: requiredIdValues } },\n    select: { id: true, key: true, version: true },\n  });\n  if (rows.length !== 4) {\n    return res.status(404).json(err(\"CONSENT_VERSION_NOT_FOUND\", \"Consent version id not found\"));\n  }\n\n  const byId = new Map(rows.map((r) => [r.id, r] as const));\n  const expectations = [\n    { key: \"disclaimer\" as const, id: consent_version_ids.disclaimer, text: consent_disclaimer_version },\n    { key: \"privacy\" as const, id: consent_version_ids.privacy, text: consent_privacy_version },\n    { key: \"refund\" as const, id: consent_version_ids.refund, text: consent_refund_version },\n    { key: \"cookie\" as const, id: consent_version_ids.cookie, text: consent_cookie_version },\n  ];\n\n  for (const ex of expectations) {\n    const row = byId.get(ex.id);\n    if (!row) return res.status(404).json(err(\"CONSENT_VERSION_NOT_FOUND\", \"Consent version id not found\"));\n    if (row.key !== ex.key) return res.status(409).json(err(\"CONSENT_KEY_MISMATCH\", \"Consent key mismatch\"));\n    if (row.version !== ex.text) return res.status(404).json(err(\"CONSENT_VERSION_NOT_FOUND\", \"Consent version text mismatch\"));\n  }\n\n  const pkg = PACKAGE_MAP[package_code];\n  if (!pkg) return res.status(400).json(err(\"PACKAGE_INACTIVE\", \"Package inactive\"));\n\n  const consentAcceptedAt = new Date(consent_accepted_at);\n\n  const created = await prisma.$transaction(async (tx) => {\n    const order = await tx.order.create({\n      data: {\n        userId,\n        packageCode: package_code,\n        status: \"submitted\",\n        amountCurrency: pkg.currency,\n        amountTotal: pkg.price,\n      },\n      select: { id: true, status: true },\n    });\n\n    const report = await tx.report.create({\n      data: {\n        orderId: order.id,\n        templateCode: pkg.templateCode,\n        contentHtml: `<h2>${pkg.templateCode} - Draft</h2><p>Preparing your report...</p>`,\n        contentVersion: 1,\n        status: \"draft\",\n        pdfGenerationStatus: null,\n      },\n      select: { id: true },\n    });\n\n    await tx.consentLog.create({\n      data: {\n        userId,\n        orderId: order.id,\n        formType,\n        consentDisclaimerVersion: consent_disclaimer_version,\n        consentPrivacyVersion: consent_privacy_version,\n        consentRefundVersion: consent_refund_version,\n        consentCookieVersion: consent_cookie_version,\n        consentTimestamp: consentAcceptedAt,\n      },\n    });\n\n    return { orderId: order.id, orderStatus: order.status, reportId: report.id };\n  });\n\n  return res.status(200).json(\n    ok({\n      order_id: created.orderId,\n      order_status: created.orderStatus,\n      report_id: created.reportId,\n      report_status: \"draft\",\n      amount_total: { currency: pkg.currency, value: pkg.price },\n    })\n  );\n}\n