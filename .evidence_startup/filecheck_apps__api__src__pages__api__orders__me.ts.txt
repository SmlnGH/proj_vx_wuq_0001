PATH=/home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/me.ts
test -f RESULT=true
wc -l:
0 /home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/me.ts
sha256sum:
a33f8007b6dfe92f047eee3516b11bc2693edd072a1f0e8dfb3112ba34ed6a83  /home/openclaw/.openclaw/workspace/proj_vx_wuq_0001_repo/apps/api/src/pages/api/orders/me.ts
HEAD20:
import type { NextApiRequest, NextApiResponse } from \"next\";\nimport { prisma } from \"../../../../lib/prisma\";\nimport { ok, err } from \"@shared/types/api\";\n\nfunction pdfUi(status: any) {\n  if (!status) return \"pending\";\n  if (status === \"ready\") return \"ready\";\n  if (status === \"failed\") return \"failed\";\n  return \"pending\";\n}\n\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  if (req.method !== \"GET\") return res.status(405).end();\n\n  const userId = (req.headers[\"x-user-id\"] as string | undefined) ?? null;\n  if (!userId) return res.status(401).json(err(\"UNAUTHORIZED\", \"Unauthorized\"));\n\n  const orders = await prisma.order.findMany({\n    where: { userId },\n    include: { payments: true, report: true, appointment: true },\n    orderBy: { createdAt: \"desc\" },\n  });\n\n  const items = orders.map((o) => {\n    const report = o.report;\n    const report_status = report?.status ?? null;\n    const pdf_generation_status = pdfUi(report?.pdfGenerationStatus);\n\n    const can_view_report = !!report && report.status === \"published\";\n    const can_download_pdf = can_view_report && pdf_generation_status === \"ready\" && !!report?.pdfStorageKey;\n\n    const payment_status = o.payments?.status ?? null;\n\n    return {\n      order_id: o.id,\n      package_code: o.packageCode,\n      order_status: o.status,\n      payment_status,\n      report_status,\n      pdf_generation_status,\n      created_at: o.createdAt.toISOString(),\n      published_at: report?.publishedAt ? report.publishedAt.toISOString() : null,\n      can_view_report,\n      can_download_pdf,\n      appointment_summary: o.appointment\n        ? {\n            start_at: o.appointment.startAt.toISOString(),\n            timezone: o.appointment.timezone,\n            status: o.appointment.status,\n          }\n        : undefined,\n    };\n  });\n\n  return res.status(200).json(ok({ items }));\n}\n